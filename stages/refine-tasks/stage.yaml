# Refine Tasks Stage - Iteratively improve tasks
# Agent decides when tasks are ready

name: refine-tasks
description: Iteratively refine tasks until agent judges them ready

termination:
  type: judgment
  min_iterations: 2
  consensus: 2

delay: 3
prompt: prompts/task-refiner.md
