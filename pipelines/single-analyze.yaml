name: single-analyze
description: Single provider test

nodes:
  - id: analyze
    swarm:
      providers: [claude]
      stages:
        - name: improve
          stage: improve-plan
          termination:
            type: fixed
            iterations: 1
